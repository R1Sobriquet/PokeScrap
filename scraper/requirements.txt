# Playwright (paquet Python) est épinglé ci-dessous ; les navigateurs + deps système
# sont installés dans scraper/Dockerfile via `playwright install --with-deps chromium`.
APScheduler==3.11.0
playwright==1.49.0
